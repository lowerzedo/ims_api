"""Admin registrations for shared models (currently none)."""
from __future__ import annotations

from django.contrib import admin

# Add shared admin registrations here when concrete models are introduced.
